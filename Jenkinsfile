pipeline {

    agent any
    stages{
         stage("Build"){
             steps{
              echo("Build")
    }
}
   stage("Run UTs"){
             steps{
              echo("Run uts")
    }
}
   stage("Deply to dev"){
             steps{
              echo("dev deployment")
    }
}
   stage("Deply to qa"){
             steps{
              echo("QA deployment")
    }
}
   stage("Run automation regr test"){
             steps{
              echo("REgression automation")
    }
}
 stage("Deploy to stage"){
             steps{
              echo("stage deployment")
    }
}
 stage("prod env"){
             steps{
              echo("deploy env")
    }
}


}
}

